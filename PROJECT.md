# 🎯 项目定位与需求

> **用法：** 改方向、定功能、记重要约束，都在这里修改。具体执行进度放在 [TASKS.md](TASKS.md)。

## 项目定位【总体方向】

- **服务对象：** 模拟 IC 芯片公司的研发人员。
- **项目目标：** 在现有研发工作基础上，通过 AI 辅助文件、知识和日常事务处理，提高个人工作效率。
- **产品边界：** 不涉及具体专业 EDA 软件及其核心专业能力。
- **运行环境：** 公司本地内网，不连接外网；研发阶段允许按规则联网调研和准备安全演示文件。
- **应用形式：** 本地客户端，不开发为网页版本。
- **基本要求：** 简单、美观、方便使用，并支持后续版本升级和功能迭代。

## 总体需求【做什么、做到什么程度】

**首版范围：** 下列总体需求均作为产品候选范围；开发前先完成开源项目分析，再由用户确认首版功能清单。
**明确不做：** 具体专业 EDA 软件功能、依赖公网运行的能力。

| 编号 | 需求 | 状态 | 满足条件 |
| --- | --- | --- | --- |
| R-001 | 提供清楚、简洁、方便 AI 接续开发的项目管理文件 | 已确认 | 定位、介绍、说明、任务、更新、关注点和新增任务均有明确入口 |
| R-002 | 合理规划模型与 Token 使用，同时保证质量和准确度 | 已确认 | 执行前按步骤选择适合的模型；常规工作控制成本，关键工作使用足够能力并完成验证 |
| R-003 | 版本记录只保留重要更新，并持续凝练历史内容 | 已确认 | 简单修改不写入版本记录；新增记录时合并重复或同类历史项，保留重要结果、影响和必要时间信息 |
| R-004 | 参考多个成熟、高 Star 的 GitHub 项目进行设计和开发 | 已确认 | 开发前完成分析并形成拟开发功能清单；用户确认后开始开发；每项新功能优先查找成熟参考，没有合适参考时再自行设计 |
| R-005 | 提供简洁美观、具有科技感的客户端界面 | 已确认 | 整体布局清楚，可参考苹果风格；支持暗黑、日常等多种皮肤和可配置个人头像 |
| R-006 | 支持多个模型 API 的配置和管理 | 已确认 | 可添加多个本地或内网可达的 API、手动选择和切换，并显示各模型 API 的当前状态及可用性检测结果 |
| R-007 | 提供基础个人工作台和本地邮箱功能 | 已确认 | 包含合理的常规工作台能力；可添加本地或内网邮箱地址并完成基本收发邮件操作 |
| R-008 | 管理本地项目文件并提供 AI 检索分析 | 已确认 | 可添加本地项目文件夹，阅读查看目录文件，并通过 AI 检索、查阅和分析其中内容 |
| R-009 | 使用 AI 分析 Datasheet | 已确认 | 提取芯片关键信息，生成包含核心内容的概略版 Datasheet，并支持预览和导出 |
| R-010 | 提供个人 Skills 管理平台 | 已确认 | 可在允许的本地或内网来源中管理、导入和导出 Skills，并可调用 AI 使用 Skills 提升个人工作流效率 |
| R-011 | 将常用文件转换为 Markdown | 已确认 | 支持 PDF、Word、TXT 等格式，转换结果可预览和导出，输出位置可临时选择或设置默认值 |
| R-012 | 提供独立 AI 对话和浮空助手 | 已确认 | 有独立日常问答界面；浮空 AI 机器人可结合当前界面内容回答问题 |
| R-013 | 支持安全、可迭代的版本与交付流程 | 已确认 | 仅用户确认完成的版本可标记为正式版；Demo 与正式交付分目录；正式版完成全面检查并清除个人及开发电脑信息 |
| R-014 | 增加与项目目录绑定的 IC 项目进度管理 | 已确认 | 每个项目必须关联用户授权的本地项目目录；可编辑名称、起止日期、阶段、里程碑、本周/当前重点任务、进度和风险；关联日历待办，并可基于用户明确提交的项目摘要调用已配置模型给出带限制说明的建议 |
| R-015 | 增加跟随电脑日期的任务与提醒界面 | 已确认 | 提供接近苹果日历的信息层级和月视图；可新增、编辑、完成、筛选本地待办，显示状态/优先级/项目关联；重点任务在应用内按提醒时间提示；日期跟随电脑系统时钟，不读取或修改系统日历内容 |
| R-016 | 增加本地科学计算器 | 已确认 | 在工作台合适入口提供不使用 `eval` 的表达式计算、常用科学函数、常量、角度模式、历史记录和明确错误提示；不把计算内容发送网络 |
| R-017 | 增加常用软件快捷管理 | 已确认 | 用户通过文件选择器显式添加本机 `.app` 或普通可执行文件，保存名称与路径并支持移除；点击后由 Rust 校验原路径仍为允许类型再启动，不接受命令字符串、参数、URL 或任意 Shell |
| R-018 | 统一优化客户端字号与视觉层级 | 已确认 | 目录/导航字号适当收紧，正文、说明、表单和状态文字提高到清晰可读范围；用统一字号、行高、间距和响应式规则保证深浅主题协调，不以缩小字号换取信息密度 |
| R-019 | 扩展项目标记、备注与历史分析 | 已确认 | 项目可收藏星标并记录手动备注；每次保存进度生成带时间的只读历史快照，可查看既往工作并由用户显式选择历史摘要交给已连接模型分析；删除项目时同步清理对应历史 |
| R-020 | 增强 Datasheet 原件阅读与 AI 概略 | 已确认 | 导入后可分页预览原 PDF；概略版支持用户显式调用已连接 AI，按 Datasheet 常用信息层级输出器件概述、特性、关键参数、引脚/封装、应用、限制和来源页，并以统一版式预览/导出；不得把 AI 输出表示为原厂数据 |
| R-021 | 增加本地个人知识笔记 | 已确认 | 提供类似 OneNote 的“笔记本/列表/编辑区”布局；本地映射存储目录可由用户选择；支持 Markdown 安全编辑与预览、标题/强调/列表/表格/本地图片等简单排版，导入 Markdown/TXT/DOCX/PDF 为新笔记，并导出 Markdown、PDF、DOC、DOCX |
| R-022 | 完善 Skills 独立清单与概述 | 已确认 | Skills 页面始终展示已导入 Skill 的独立可搜索列表、启停状态、版本、来源、权限和从描述/正文生成的简短作用概述；概述不执行 Skill 内容 |
| R-023 | 完善邮件文档预览与常规操作 | 已确认 | 邮件详情支持常见文本及 PDF/DOCX/TXT/Markdown 附件的安全预览，并保留写信、回复、转发、搜索、附件导出和二次确认发送；不加载远程 HTML 资源、不执行附件内容 |
| R-024 | 优化日历日期与事件交互 | 已确认 | 用系统时钟自动保持当前日期和“今天”高亮，无单独同步按钮且不在后台读取系统日历内容；双击已有待办打开编辑，紧急日程使用加粗高对比样式，单击仍可查看/编辑以兼容触控和键盘 |
| R-025 | 优化科学计算器初态与按键 | 已确认 | 首次及再次启动保持空白输入，不预填表达式；可直接键入常规数学表达式；扩展括号、退格、函数、常量和运算按键，保持安全解析、本地历史及明确错误提示 |
| R-026 | 提升 Demo 全功能展示完整度 | 已确认 | Demo 为项目、进度历史、日历、Datasheet、笔记、Skills、邮箱、计算器和软件入口提供明确的合成示例或演示预置；配套输入/导出样例统一放在一个演示资料目录，绝不包含真实数据或开发机路径 |
| R-027 | 将当前成果净化、验收并发布为首个正式版本 | 已确认 | v1.0.0 的普通版和 Demo 均可点击启动且覆盖已确认功能；仓库与安装包不含密钥、个人资料、绝对开发路径、缓存或临时输出；README 按项目案例模板说明场景、价值、安全、成熟度、下载和快速使用；完整源码及发布包上传用户 GitHub |

新增需求复制一行，编号从 **R-028** 递增。状态使用“待确认 / 已确认 / 已取消”；开发进度统一看任务表。

基础个人工作台的“常规功能”尚需在参考项目分析后细化，不能在未确认时自行扩大范围。

## 首版开发功能清单【已确认】

> 来源：T-009 开源项目调研和 R-005 至 R-012。用户已于 2026-09-05 明确确认按此范围推进；由 T-002 选择技术方案并开始实施。

| 模块 | 首版功能与验收边界 | 对应需求 |
| --- | --- | --- |
| 客户端框架与首页 | 本地客户端可启动；左侧统一导航；首页显示最近项目、最近文档、最近对话、模型/邮箱状态及快捷入口；支持日常浅色、科技深色和本地头像 | R-005、R-007 |
| 模型 API 管理 | 添加、编辑、停用本地或内网可达的 OpenAI 兼容端点；密钥安全存储；手动选择模型；连接检测显示成功、失败原因和检测时间 | R-006 |
| AI 对话与浮空助手 | 独立多会话对话、流式输出、Markdown 展示、文件附件和模型切换；浮空窗口仅读取用户明确选择的当前应用页面内容，不默认读取系统其他内容 | R-012 |
| 本地项目与检索 | 添加/移除本地文件夹授权；目录浏览、文件预览、增量索引、关键词与语义检索；回答提供文件名和位置引用；首版不修改项目源文件 | R-008 |
| Datasheet 分析 | 导入本地 PDF；保留页码、表格、单位和条件；提取器件概述、特性、关键参数、引脚/封装、应用、限制与来源；生成概略版并预览、导出 Markdown/PDF | R-009 |
| Skills 管理 | 从本地或内网受控来源导入、查看、启停、导出 Skill；校验清单、版本、来源和权限；首版默认只支持说明/提示词类 Skill，含脚本 Skill 不自动执行 | R-010 |
| 文件转 Markdown | 支持 PDF、DOCX、TXT；单文件/批量转换、结果预览、另存为和默认输出目录；失败时保留原文件并给出明确原因 | R-011 |
| 本地邮箱 | 配置内网 IMAP/SMTP 账户；同步文件夹和邮件列表、阅读、搜索、写信、回复/转发、附件收发；首版不含日历、延迟发送、营销追踪和复杂规则 | R-007 |
| 本地数据与安全 | 配置、索引、历史和日志保存在本机；提供备份/恢复入口、离线状态提示、敏感日志脱敏、网络访问总开关和模块级权限说明 | R-006 至 R-013 |
| 项目进度与日历 | 项目目录与进度卡一一关联；编辑阶段、里程碑、重点、风险和百分比；本地待办以项目关联键双向展示，支持月视图、状态、优先级和应用内提醒；当前日期自动跟随系统时钟 | R-014、R-015 |
| 科学计算器 | 安全表达式解析、科学函数/常量、角度模式、计算历史；固定放在侧栏工具区，不访问网络 | R-016 |
| 常用软件 | 显式选择、卡片式快捷入口、启动和移除；后端只启动经过重新校验的应用包或普通可执行文件，不执行任意命令 | R-017 |
| 个人知识笔记 | OneNote 风格三栏管理、本地映射目录、Markdown 安全编辑/预览、图片/表格/文本、文档导入和 Markdown/PDF/DOC/DOCX 导出 | R-021 |
| 体验与功能深化 | 全局字体层级；项目历史/星标/备注；PDF 原件与 AI 概略；Skills 清单；邮件附件预览；日历双击/紧急样式；计算器丰富键盘；全功能 Demo | R-018 至 R-020、R-022 至 R-026 |

### R-018 至 R-026 调研后的设计边界（2026-09-06）

- 视觉层级沿用现有 CSS 变量体系集中治理，导航/目录适度收紧，正文、表单和辅助说明建立最低可读字号；不引入大型 UI 框架或复制第三方主题。
- 项目历史参考 Plane/OpenProject 的活动与工作包思路，保存为追加式快照而非覆盖旧记录；星标和备注属于项目元数据，AI 只分析用户明确点击时提交的受限历史摘要。
- PDF 原件阅读采用随包 PDF.js 的受限 Canvas 分页/缩放；worker、字体与 CMap 全部本地打包，不加载在线查看器或执行 PDF 脚本；概略版继续保留页码、单位和条件。
- 笔记参考 Joplin 的离线优先 Markdown、笔记本/笔记列表和导入导出模型，以及 Tiptap 的工具栏与表格交互；首轮不引入 Tiptap 付费扩展，使用安全 Markdown 作为唯一可移植事实源，本地 PNG/JPEG/WebP 在大小校验后以 `data:image` 嵌入笔记，预览层继续阻止远程图片。
- DOC 是兼容 Word 打开的 HTML 文档，DOCX 是 OOXML；两者在界面中明确区分。导入统一复用现有受限文档解析器，不执行宏、脚本、嵌入对象或 PDF 动作。
- Skills 参考 Open WebUI 的“轻量清单 + 按需完整内容”理念，但不复用其带额外条款代码；默认列表仅呈现经过校验的元数据和概述。
- 邮件参考 Thunderbird 的三栏阅读和附件操作，仅增加本机安全预览；HTML 邮件、远程图片、附件宏和可执行内容继续不渲染/不执行。
- 日历“跟随电脑”解释为跟随系统时钟保持日期正确，不进行无提示的系统日历后台读取；既有系统日历主动同步入口移除。Demo 参考 Storybook 的组件状态样例理念，在 Demo 模式由后端幂等注入合成数据，不污染正常版。
- 计算器参考 Qalculate/SpeedCrunch 的“直接键入表达式 + 丰富函数”体验，只参考功能范围，不复用 GPL 代码。

### R-014 至 R-017 调研后的设计边界（2026-09-06）

- IC 项目管理不复制 EDA 流程本身：参考 SiliconCompiler 的项目清单/flowgraph/provenance 和 OpenROAD 的阶段流水线，只采用“项目目录、阶段、里程碑、结果来源”这些管理概念；任务、进度与风险字段参考 OpenProject 的 work package、日期、里程碑和完成度模型。
- 日历由应用自己的本地待办作为可编辑事实源；电脑只提供系统时钟，不读取或修改系统日历，因而不申请日历权限。项目重点任务通过 `project_id` 关联待办，不靠标题模糊匹配。
- 提醒首版采用应用内到期提示：重点/高优先级任务到达提醒时间后在工作台和日历醒目显示；不新增后台常驻 Agent，也不在应用退出时承诺系统通知。
- AI 建议只发送用户明确选择的项目管理字段和关联待办摘要，不默认发送项目文件正文；必须先通过模型连接检测，回答标注“建议”且不自动修改项目或任务。
- 科学计算器自行实现小型安全解析器，参考 Qalculate 的函数/常量/单位友好思路，但首版不复用 GPL 代码、不做符号积分、绘图或汇率。
- 软件快捷入口参考 Wox 的本地快速启动体验，但首版不接受 Shell、参数、URL、插件或自动扫描；所有目标由用户选择，Rust 启动前重新检查类型，最大限度缩小命令执行面。

### 建议实施顺序

1. 客户端框架、本地数据层、主题与首页。
2. 模型 API 管理和独立 AI 对话。
3. 文件转换、项目索引与引用式检索。
4. Datasheet 结构化分析与导出。
5. Skills 管理和浮空助手。
6. 本地邮箱及全功能 Demo 集成。

### 暂缓到后续版本

- 多人协作、账号权限后台、跨设备云同步、在线插件/Skill 市场。
- 自动操作项目文件、自动发送邮件、无人值守 Agent 和脚本类 Skill 自动执行。
- 邮件日历、延迟发送、阅读追踪、复杂规则，以及动态背景、透明窗口等非核心视觉效果。

## 技术方案【T-002 已确定】

### 总体架构

| 层级 | 方案 | 职责与边界 |
| --- | --- | --- |
| 桌面与系统层 | Tauri 2 / Rust | 窗口、文件授权、网络策略、进程管理、系统凭据、更新包校验；按窗口配置最小 capability |
| 界面层 | React / TypeScript / Vite / 原生 CSS 变量 | 工作台、主题、对话、文档和设置界面；不直接访问任意文件、密钥或启动外部进程 |
| 应用服务层 | Rust 模块化服务 | 暴露类型明确的命令，承接模型适配、项目索引、邮件、备份和审计；不向前端开放任意 SQL 或 Shell |
| 本地数据层 | SQLite + 版本化迁移 | 保存配置元数据、会话、索引、任务状态和审计信息；模型密钥、邮箱密码不进入普通数据库 |
| 凭据层 | 操作系统凭据库，必要时使用 Stronghold 兼容回退 | 保存 API 密钥和邮箱凭据；界面只显示脱敏值，不通过日志回显 |
| 文档处理层 | Rust 类型化解析模块 | 在后端按格式、签名、大小和用户授权路径校验后处理 PDF、DOCX、TXT/Markdown；不执行宏、脚本或嵌入对象 |
| 搜索层 | SQLite FTS 起步，可替换本地向量索引 | 首版先保证可解释关键词检索；语义检索通过适配接口增量加入，结果统一保留文件与页码引用 |

### 选择理由

- Tauri 使用系统 WebView，安装体积和常驻资源更适合个人内网客户端；Rust 后端和 capability 可将文件、网络、窗口、数据库及解析权限缩到具体模块。
- React/TypeScript 适合快速构建信息密度较高的桌面界面；首版不引入大型 UI 框架，用设计令牌和基础组件控制依赖及主题一致性。
- SQLite 便于单机备份、迁移和全文检索；业务数据只通过 Rust 的类型化仓储访问，避免前端获得任意数据库写权限。
- 文档只通过 Rust 类型化命令解析；格式、文件签名、路径和资源上限在后端复核，失败不修改源文件，也不向界面开放任意文件读取。
- Electron 保留为兼容性备选，但其 Chromium/Node 交付体积、更新面和渲染进程安全维护成本对本项目没有明显收益。

### 目录与交付约定

- `app/`：正式产品源代码；当前首先建立 `app/desktop/`。
- `demo/`：后续完整功能演示及合成/脱敏示例数据，不与正式产品数据混放。
- `release/`：仅放用户确认后的正式交付物；开发阶段不创建正式版本标记。
- 依赖必须锁定版本并保留许可证清单；内网发布使用签名离线安装包和可选的内网更新源，默认关闭公网更新检查。

### 原型验证门槛

- T-003 必须验证前端构建、单元测试和 Tauri 配置；具备 Rust 工具链时再验证桌面窗口启动。
- T-012 前用合成 Datasheet 检查页码、表格、单位、条件与导出准确性，再决定 Docling/MarkItDown 的实际版本与组合。
- 邮件、脚本 Skill、浮空助手和外部模型连接必须先完成威胁建模与权限测试，不因界面完成而视为功能完成。

## ⚠️ 重要关注点【约束与待解决问题】

- **开发位置：** 所有项目开发均在当前文件夹及子目录内进行。
- **管理方式：** 文件按用途分工，保持简洁，避免重复记录同一状态。
- **开发顺序：** 先研究参考项目并结合本项目需求形成清单，经用户确认后再实现。
- **参考安全：** 下载的开源文件必须隔离存放，完成安全检查后才能使用；发现恶意内容时先停止执行，在隔离区清理或修复并重新检查。
- **参考留档：** 每版完成后删除隔离区内下载文件，保留项目名称、网址和使用说明到 `REFERENCES.md`。
- **模型成本：** 工作前按步骤规划模型；常规任务优先考虑 Token 性价比，复杂和关键任务优先保证能力匹配。
- **质量底线：** 成本优化不能牺牲质量和准确度；关键结果必须验证，有疑问时使用更可靠的模型复核。
- **更新记录：** 只记录影响功能、使用方式、关键技术、数据兼容、重要修复或管理规则的变化；每次更新都检查并凝练同类历史项。
- **工程资料：** 真实芯片资料和密钥不提交仓库；开发示例使用合成或脱敏数据。
- **结果准确：** 工程数据保留来源、单位和条件；未验证的 EDA 结果注明限制。
- **网络边界：** 成品在本地内网离线运行；开发调研和 Demo 素材下载与成品运行环境分开管理。
- **发布规则：** 过程完善和迭代不算正式版本；只有用户明确确认后才能标记正式版本。
- **交付质量：** Demo 覆盖全部功能；正式版检查功能、安全和可移植性，并清除个人信息与开发电脑痕迹。
- **待验证：** 已完成 Datasheet 合成样本、邮件/模型凭据隔离、浮空助手逐页授权及去除开发路径的候选应用包验证；正式交付仍须验证目标平台正式签名安装包中的凭据生命周期，以及公司测试 IMAP/SMTP/模型服务的证书与认证兼容性。
- **公开发布边界：** GitHub 仓库只包含源码、必要文档和合成 Demo 资料；构建缓存、运行数据库、个人配置、真实工程资料与凭据不进入版本控制，二进制通过 GitHub Release 独立分发。

新增关注点直接加一条：`- **主题：** 说明。`

## 📌 关键决定【决定了什么、为什么】

只记影响后续工作的决定，每条一句话即可。

| 日期 | 决定 | 原因 |
| --- | --- | --- |
| 2026-09-06 | 用户选择 MIT 许可证，保留第三方许可证与字体授权；公司真实服务和公证签名验收单列 T-034 | 便于公开下载与再利用，同时明确本地验证不代表目标企业环境已通过 |
| 2026-09-05 | 产品采用面向模拟 IC 研发人员的本地内网客户端，不接入具体 EDA 软件 | 明确目标用户、部署边界和架构方向 |
| 2026-09-05 | 先参考成熟 GitHub 项目并确认功能清单，再进入开发 | 吸收成熟设计，控制范围并减少重复开发 |
| 2026-09-05 | 正式版本必须由用户确认，并在交付前完成全面检查与信息清理 | 保证版本定义清楚且可安全移交 |
| 2026-09-05 | 版本记录仅保留重要变化，并在每次更新时合并同类历史内容 | 保留有效信息，避免记录随开发过程持续膨胀 |
| 2026-09-05 | 按任务步骤规划模型，兼顾 Token 性价比、质量和准确度 | 避免简单工作过度消耗，同时为关键工作保留足够的模型能力 |
| 2026-09-05 | 使用项目总览、需求、任务、版本记录和 AI 规则这五份文件协作 | 入口明确，便于修改和 AI 接续工作 |
| 2026-09-05 | 首版采用 Tauri 2、React/TypeScript、Rust 类型化服务和 SQLite 的分层架构，文档解析落在受限 Rust 模块 | 满足本地内网、最小权限、轻量桌面体验，减少额外运行时并保留后续模块替换能力 |
| 2026-09-05 | 模型端点由 Rust 校验并固定内网 DNS 解析结果、禁止重定向，密钥只存操作系统凭据库 | 降低前端绕过、SSRF、DNS 重绑定和凭据泄露风险 |
| 2026-09-05 | Chat Completions 的 SSE 流由 Rust 解析并以带请求标识的事件推送界面，会话通过类型化仓储保存到带版本迁移的 SQLite | 保持网络与数据权限边界，同时支持流式体验和本机恢复 |
| 2026-09-05 | 对话图片只接受用户通过文件选择器明确授权的 PNG/JPEG/WebP，Rust 复核内容且不保存原始路径 | 控制任意文件读取、伪造类型、超大请求和本地路径泄露风险 |
| 2026-09-05 | 模型回答使用关闭原始 HTML、经过白名单清洗的 GFM 渲染，回答中的链接和图片引用默认只读 | 防止不可信模型输出执行脚本或自动访问内外网资源 |
| 2026-09-05 | 通用文档附件复用 T-012/T-013 的隔离解析与统一文档中间表示，对话只引用用户明确选择的文档及受限文本片段 | 避免在对话模块重复解析器、直接上传原文件或绕过文件授权与来源引用 |
| 2026-09-06 | 模型和邮箱共用默认关闭的网络访问总开关，模块命令仍分别执行内网 DNS、TLS、凭据和人工操作检查 | 提供可见的完全离线状态，并避免只靠界面禁用形成可绕过的安全边界 |
| 2026-09-06 | `.icwb` 备份使用 SQLite 在线备份，包含本地业务数据和经过校验的界面配置快照，但不包含系统凭据，恢复后强制离线 | 兼顾单机迁移、数据库一致性和密钥隔离，避免恢复旧配置后自动产生网络访问 |
| 2026-09-06 | 邮件同步采用 IMAP `EXAMINE`、UID 与 `BODY.PEEK[]`，发送采用 SMTPS/强制 STARTTLS 并要求独立二次确认 | 首版默认只读获取邮件，防止预览改变服务端已读状态或 AI/草稿自动发送 |
| 2026-09-06 | 正式候选包必须使用编译路径重映射构建，并在签名前后扫描私密文件、符号链接、用户名和开发路径 | 源码扫描无法发现 Rust 依赖写入二进制的本机 Cargo 路径，需要对最终产物独立验收 |
| 2026-09-06 | 用户确认当前成果作为 v1.0.0 首个正式版本，并以普通版、全功能 Demo、完整源码和 GitHub Release 方式交付 | 将可复现源码、可点击应用和集中演示资料分开管理，便于下载试用及后续迭代 |
