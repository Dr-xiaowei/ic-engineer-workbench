# 项目要求与发布规范

## 1. 定位与当前基线

本地项目根目录为 `company-intranet-workbench/`，独立保存 Git 历史、源码、管理文件、Demo 和正式交付物；外层目录提供跨项目通用规范和导航。所有项目相对路径以该目录为起点。

**范围说明：** 本文件完整保留此前的 R-001 至 R-029 需求。模拟 IC 用户、公司内网、本地客户端、Datasheet/模型 API/Skills/邮箱/笔记/日历等功能，以及 Tauri/React/Rust/SQLite、MIT 和指定发布仓库，都属于本项目；不自动用于后续平台。通用方法已提炼到外层 PROJECT.md 与 AGENTS.md，本项目保留采用基线和实际执行细则，确保独立克隆时可继续开发。

面向模拟 IC 研发人员的本地 AI 工作台，辅助资料、知识与日常事务管理，不接入具体 EDA 工具，不替代工程签核。成品在本地或公司内网运行，不依赖公网；开发调研和依赖准备可按安全规则联网。

- 当前正式版本：v1.0.0，MIT 许可证，已获用户确认并发布。
- 预编译目标：macOS Apple Silicon；普通版和 Demo 使用独立标识及数据目录。
- [源码仓库](https://github.com/Dr-xiaowei/ic-engineer-workbench)与[正式版本下载](https://github.com/Dr-xiaowei/ic-engineer-workbench/releases)统一管理源码和交付物。
- 功能使用看 [README](README.md)，逐项验收看[完成度报告](COMPLETION_REPORT.md)，当前任务看 [TASKS](TASKS.md)。正式发布不等于公司生产环境已验收。

## 2. 已确认需求

R-001 至 R-027 源自 v1.0.0 的确认基线，其中 R-004 已于 2026-09-19 升级为后续选型规范；R-028 为持续执行的版本管理要求。编号保持稳定，不因文档整理重排或复用。下表“已确认”表示范围获准，完成与验证状态以报告和任务表为准。

| 编号 | 需求 | 状态 | 满足条件 |
| --- | --- | --- | --- |
| R-001 | 提供清楚、简洁、方便 AI 接续开发的项目管理文件 | 已确认 | 定位、介绍、说明、任务、更新、关注点和新增任务均有明确入口 |
| R-002 | 合理规划模型与 Token 使用，同时保证质量和准确度 | 已确认 | 执行前按步骤选择适合的模型；常规工作控制成本，关键工作使用足够能力并完成验证 |
| R-003 | 版本记录只保留重要更新，并持续凝练历史内容 | 已确认 | 简单修改不写入版本记录；新增记录时合并重复或同类历史项，保留重要结果、影响和必要时间信息 |
| R-004 | 开发前优先搜索并评估现有开源项目、Skill 或类似方案 | 已确认 | 先不写代码、不默认从零开发；优先近期维护、文档完整、社区使用较多的候选，按十项标准核对维护/社区/部署/依赖/复用/缺口/安全/许可证；比较 A 直接使用、B 二次开发、C 从零开发并明确推荐及原因，提出最小 MVP，等待用户确认方案与范围后才实施 |
| R-005 | 提供简洁美观、具有科技感的客户端界面 | 已确认 | 整体布局清楚，可参考苹果风格；支持暗黑、日常等多种皮肤和可配置个人头像 |
| R-006 | 支持多个模型 API 的配置和管理 | 已确认 | 可添加多个本地或内网可达的 API、手动选择和切换，并显示各模型 API 的当前状态及可用性检测结果 |
| R-007 | 提供基础个人工作台和本地邮箱功能 | 已确认 | 包含合理的常规工作台能力；可添加本地或内网邮箱地址并完成基本收发邮件操作 |
| R-008 | 管理本地项目文件并提供 AI 检索分析 | 已确认 | 可添加本地项目文件夹，阅读查看目录文件，并通过 AI 检索、查阅和分析其中内容 |
| R-009 | 使用 AI 分析 Datasheet | 已确认 | 提取芯片关键信息，生成包含核心内容的概略版 Datasheet，并支持预览和导出 |
| R-010 | 提供个人 Skills 管理平台 | 已确认 | 可在允许的本地或内网来源中管理、导入和导出 Skills，并可调用 AI 使用 Skills 提升个人工作流效率 |
| R-011 | 将常用文件转换为 Markdown | 已确认 | 支持 PDF、Word、TXT 等格式，转换结果可预览和导出，输出位置可临时选择或设置默认值 |
| R-012 | 提供独立 AI 对话和浮空助手 | 已确认 | 有独立日常问答界面；浮空 AI 机器人可结合当前界面内容回答问题 |
| R-013 | 支持安全、可迭代的版本与交付流程 | 已确认 | 仅用户确认完成的版本可标记为正式版；Demo 与正式交付分目录；正式版完成全面检查并清除个人及开发电脑信息 |
| R-014 | 增加与项目目录绑定的 IC 项目进度管理 | 已确认 | 每个项目必须关联用户授权的本地项目目录；可编辑名称、起止日期、阶段、里程碑、本周/当前重点任务、进度和风险；关联日历待办，并可基于用户明确提交的项目摘要调用已配置模型给出带限制说明的建议 |
| R-015 | 增加跟随电脑日期的任务与提醒界面 | 已确认 | 提供接近苹果日历的信息层级和月视图；可新增、编辑、完成、筛选本地待办，显示状态/优先级/项目关联；重点任务在应用内按提醒时间提示；日期跟随电脑系统时钟，不读取或修改系统日历内容 |
| R-016 | 增加本地科学计算器 | 已确认 | 在工作台合适入口提供不使用 `eval` 的表达式计算、常用科学函数、常量、角度模式、历史记录和明确错误提示；不把计算内容发送网络 |
| R-017 | 增加常用软件快捷管理 | 已确认 | 用户通过文件选择器显式添加本机 `.app` 或普通可执行文件，保存名称与路径并支持移除；点击后由 Rust 校验原路径仍为允许类型再启动，不接受命令字符串、参数、URL 或任意 Shell |
| R-018 | 统一优化客户端字号与视觉层级 | 已确认 | 目录/导航字号适当收紧，正文、说明、表单和状态文字提高到清晰可读范围；用统一字号、行高、间距和响应式规则保证深浅主题协调，不以缩小字号换取信息密度 |
| R-019 | 扩展项目标记、备注与历史分析 | 已确认 | 项目可收藏星标并记录手动备注；每次保存进度生成带时间的只读历史快照，可查看既往工作并由用户显式选择历史摘要交给已连接模型分析；删除项目时同步清理对应历史 |
| R-020 | 增强 Datasheet 原件阅读与 AI 概略 | 已确认 | 导入后可分页预览原 PDF；概略版支持用户显式调用已连接 AI，按 Datasheet 常用信息层级输出器件概述、特性、关键参数、引脚/封装、应用、限制和来源页，并以统一版式预览/导出；不得把 AI 输出表示为原厂数据 |
| R-021 | 增加本地个人知识笔记 | 已确认 | 提供类似 OneNote 的“笔记本/列表/编辑区”布局；本地映射存储目录可由用户选择；支持 Markdown 安全编辑与预览、标题/强调/列表/表格/本地图片等简单排版，导入 Markdown/TXT/DOCX/PDF 为新笔记，并导出 Markdown、PDF、DOC、DOCX |
| R-022 | 完善 Skills 独立清单与概述 | 已确认 | Skills 页面始终展示已导入 Skill 的独立可搜索列表、启停状态、版本、来源、权限和从描述/正文生成的简短作用概述；概述不执行 Skill 内容 |
| R-023 | 完善邮件文档预览与常规操作 | 已确认 | 邮件详情支持常见文本及 PDF/DOCX/TXT/Markdown 附件的安全预览，并保留写信、回复、转发、搜索、附件导出和二次确认发送；不加载远程 HTML 资源、不执行附件内容 |
| R-024 | 优化日历日期与事件交互 | 已确认 | 用系统时钟自动保持当前日期和“今天”高亮，无单独同步按钮且不在后台读取系统日历内容；双击已有待办打开编辑，紧急日程使用加粗高对比样式，单击仍可查看/编辑以兼容触控和键盘 |
| R-025 | 优化科学计算器初态与按键 | 已确认 | 首次及再次启动保持空白输入，不预填表达式；可直接键入常规数学表达式；扩展括号、退格、函数、常量和运算按键，保持安全解析、本地历史及明确错误提示 |
| R-026 | 提升 Demo 全功能展示完整度 | 已确认 | Demo 为项目、进度历史、日历、Datasheet、笔记、Skills、邮箱、计算器和软件入口提供明确的合成示例或演示预置；配套输入/导出样例统一放在一个演示资料目录，绝不包含真实数据或开发机路径 |
| R-027 | 将当前成果净化、验收并发布为首个正式版本 | 已确认 | v1.0.0 的普通版和 Demo 均可点击启动且覆盖已确认功能；仓库与安装包不含密钥、个人资料、绝对开发路径、缓存或临时输出；README 按项目案例模板说明场景、价值、安全、成熟度、下载和快速使用；完整源码及发布包上传用户 GitHub |
| R-028 | 经用户确认的正式版本统一发布到 GitHub | 已确认 | 每次用户明确确认正式版本后，在统一仓库上传对应源码、唯一版本标签及 Release；包含普通版、Demo、重要更新说明和校验清单，核对远端内容后才标记发布完成；未确认版本不得按正式版发布 |
| R-029 | 公司内网平台独立目录与迁移完整性 | 已确认 | 统一在 company-intranet-workbench 中保存源码、文档、Git 和交付物，与后续项目隔离；迁移时校验文件、更新入口和路径，保留正式版与运行数据并执行功能回归 |

新增需求从 R-030 递增，状态使用“待确认 / 已确认 / 已取消”。新功能须先调研、整理清单并获用户明确确认；候选建议不自动进入开发范围。

## 3. 技术架构与功能边界

### 开发前选型与 MVP 门槛（R-004，2026-09-19 升级）

适用于新项目及新增功能的方案选择；不追溯重写 v1.0.0 验收结论，也不自动要求替换现有实现。

1. 先明确用户当前真正需要的场景、输入输出、目标设备及本地/内网限制；信息缺失时列出假设或询问关键问题，不擅自扩大范围。
2. 通过 GitHub 实时搜索已有开源项目、Skill 和类似解决方案，交叉比较多个候选；优先仍在维护、文档完整、社区使用较多且匹配需求的方案。高 Star 是参考而非硬性门槛，小众但适配的项目不直接排除。
3. 按 [REFERENCES 十项标准](REFERENCES.md) 记录证据、检查日期与具体版本，分别说明现成可用、可二次开发和必须新开发的部分。未知数据写“未核实”，不将历史 Star 或 README 宣称当作当前事实或实测。
4. 明确比较 **A：直接使用现有项目、B：基于现有项目二次开发、C：完全从零开发**，给出一个首选及理由，并说明另外两项为何不优先。先判断 A 能否满足核心需求，再评估 B 的真实改造与维护成本；只有现有方案在适配、安全、许可证或部署上存在证据明确的障碍时才推荐 C。不能为复用而忽略本地内网约束，也不能无调研默认从零开发。
5. 基于最终需求提出最简单 MVP：目标用户与一个核心工作流程、必要输入输出、最少核心功能、各功能的复用来源/缺口、部署依赖、验收标准和明确不做项。仅保留完成核心流程必需的能力；安全、权限、许可证与基本可用性不能作为“精简”对象。
6. 调研结束后先交付候选比较、A/B/C 推荐与 MVP，再暂停等待用户明确确认推荐方案和 MVP 范围。此前不写产品代码、不搭脚手架、不安装运行候选代码；只读调研与经检查的隔离静态查看不等于实施授权。无合适候选时报告检索范围与淘汰理由，也须等待确认，不能自动转入 C。

### 已采用架构

| 层级 | 已采用方案 | 职责 |
| --- | --- | --- |
| 客户端 | Tauri 2、系统 WebView | 桌面窗口、最小权限 IPC、系统文件选择及打包 |
| 界面 | React、TypeScript、原生 CSS | 统一导航、深浅主题、表单与安全 Markdown 展示 |
| 应用服务 | Rust 类型化模块 | 模型、项目、文档、邮箱、笔记、日历及受控软件启动 |
| 本地数据 | SQLite schema 9、FTS | 配置元数据、索引、会话、任务和历史；版本迁移与备份 |
| 凭据 | 操作系统凭据库 | 模型密钥与邮箱密码，不进入普通数据库或备份 |
| 文档 | 受限 Rust 解析、随包 PDF.js、结构化 DOCX 导出 | 保留文本来源与页码；PDF 原件本地 Canvas 阅读 |

### 必须持续保留的边界

- **网络与权限：** 网络总开关默认关闭。后端验证内网目标、固定已验证的 DNS 结果、禁止重定向并限制超时和响应大小；不能只靠前端禁用按钮。公开 GitHub 是开发发布渠道，不是成品运行依赖。
- **项目与 AI：** 工程目录只读；增量索引、关键词检索及 AI 候选分析保留来源。模型只接收用户明确提交的受限内容，AI 不自动写回项目、进度或发送邮件；浮空助手只使用当前工作台页面的显式授权信息。
- **内容安全：** 文件先检查授权路径、类型、签名、大小及压缩条目边界。Markdown 禁用原始 HTML，清洗不可信输出，阻止远程图片；不执行附件宏、PDF 脚本或 Skills 脚本。
- **项目进度：** 项目必须绑定授权目录；每次保存追加历史快照，星标与备注为元数据，日历通过项目标识关联，不以标题模糊匹配。
- **PDF 与笔记：** PDF.js worker、字体及 CMap 本地打包。笔记以 Markdown 为可编辑事实源，支持受限本地图片和简单表格；DOC 为兼容 HTML，DOCX 为 OOXML。无 OCR，不承诺复杂 Office/PDF 排版完整保真。
- **邮箱：** 只读同步使用 EXAMINE、UID 与 BODY.PEEK；发送使用 TLS 并二次确认。首版不支持服务端删除、移动和已读同步，不加载远程 HTML 资源。
- **日历与计算：** 日期跟随系统时钟，不读取或修改 Apple 日历；提醒仅在应用运行且相关任务已载入时生效。计算器空白初态，使用安全解析器，不使用 eval 或联网计算。
- **软件管理：** 仅显式添加并重新验证本地应用或普通可执行文件；拒绝 Shell、参数、URL 和符号链接。
- **数据迁移：** 备份不含系统凭据，恢复后二次确认并保持离线。普通版与 Demo 不共享数据；旧开发标识数据不自动迁移。
- **暂不纳入：** 多人协作、云同步、在线插件市场、无人值守 Agent、自动修改工程资料及具体 EDA 能力。新增范围必须重新确认。

参考项目及采用情况统一保留在 [REFERENCES](REFERENCES.md)，不在本文件重复调研流水。依赖确切版本与许可证以锁文件及随包授权资料为准。

## 4. 正式版本与 GitHub 统一管理

**每次经过用户明确确认的正式版本，都必须上传到同一 GitHub 仓库进行统一版本管理。** 不以本地打包完成、自动测试通过或一般性的“继续开发”代替正式版本确认。

1. **确认与验收：** 记录用户确认的版本号、范围和已知限制；完成相关测试、普通版初态、Demo、许可证、安全、隐私与可移植性检查。需要外部资源的验收单列，不虚报完成。
2. **准备交付：** 对齐源码版本、构建配置和版本说明；保留锁文件、必要测试与合成资料。生成普通版、Demo 及 SHA-256 清单，清除开发路径、密钥和用户资料。
3. **源码与标签：** 将该版本源码提交到统一仓库，以唯一的 `vX.Y.Z` 标签指向对应验收提交。已发布标签和附件不随意移动、覆盖或删除；修复应进入下一次经确认的版本。
4. **Release：** 同版本建立 GitHub Release，提供普通版、Demo、校验清单、简明重要更新、使用方式和已知限制；大体积安装包不放入源码历史。
5. **远端核对：** 确认仓库归属、提交、标签、附件状态与 SHA-256；具备条件时重新下载校验。只记录实际完成的检查，未下载不得写成下载验证通过。
6. **交接：** 核对成功后将发布任务标记完成，更新下载入口与完成报告。授权、网络或权限阻塞时写明“待发布”及原因，不误报已上传。

普通开发提交与文档整理不构成新正式版本，也不自动触发新标签、Release 或覆盖历史包。GitHub 保留各次已确认正式版本，不能按“清理中间文件”删除正式版本历史。

本地迁移使用 `chore/intranet-workbench-organization` 分支并保留迁移前文档快照；不改写 `main` 或 v1.0.0 标签。后续从已确认基线建立独立迭代分支，源码与交付物均留在项目目录。双包构建按配置版本选择输出目录，已有产物不覆盖；正式发布仍须执行上述确认与远端核对流程。

## 5. 文档维护与清理

| 文件或目录 | 唯一主要职责 |
| --- | --- |
| README.md | 产品介绍、下载、运行与开发入口 |
| PROJECT.md | 需求基线、技术边界、正式发布规则 |
| TASKS.md | 当前任务、待验收事项、完成阶段索引与交接 |
| CHANGELOG.md | 对使用者有意义的重要版本变化，不记录操作流水 |
| AGENTS.md | AI 执行与协作约定 |
| REFERENCES.md | 参考项目、来源、版本、许可证与安全结论 |
| COMPLETION_REPORT.md | 当前正式版逐项实现、验证证据与限制 |
| docs/ | 开发复盘及最终项目案例，保留 Word/Markdown |
| app/、demo/ | 产品源码与必要合成演示输入，不属于过程垃圾 |
| release/ | 已确认版本的本地最终应用、下载包、校验和及发布说明 |

- 小修字句、排版调整、微小重构、重复测试和授权重试不新增迭代记录。功能、使用方式、关键技术、兼容性、重要修复或管理规则变化才记录；同主题合并，一次变化不拆成多条流水。
- 已完成任务按有意义阶段归并；详细功能和证据引用报告，不重复保留各轮测试数字。有效经验保留在复盘，原始历史可由 Git 追溯，无需再建冗余归档文件。
- 只删除确认可重建的缓存、临时渲染/测试导出、参考下载副本和系统元数据；删除前检查具体路径及源码引用。保留源码、锁文件、测试、许可证、必要 Demo 输入、正式交付物及版本历史。
- 用户工程原件、案例模板、运行数据、备份和凭据不得按开发垃圾清理；本地与远端正式包也不因“只保留最终版本”而自动删除历史正式版本。

## 6. 当前未闭环事项

公司真实模型与 IMAP/SMTP、目标平台安装签名和凭据生命周期、备份恢复需按 T-034 在批准环境验收；当前 macOS 包仅 ad-hoc 签名，尚无 Developer ID 公证。Linux 依赖告警须在该平台发布前复核。具体证据与限制以 [COMPLETION_REPORT](COMPLETION_REPORT.md) 为准。
