# 🤖 公司内网工作平台开发协作约定

本文件只约束本项目。工作区外层 PROJECT.md、AGENTS.md 提供跨项目通用规范；这里保留本项目完整的执行基线，独立克隆仓库也可使用。通用规范的修改不自动取消本项目已经确认的内网、功能、技术和发布约束。

## 开始前

- 本项目根目录为 `company-intranet-workbench/`，外层提供通用规范和项目导航；源码、Git、依赖和交付物均在本项目内管理。执行命令明确设置项目工作目录，不再使用外层的旧 app、docs 或 release 路径。
- 阅读 `README.md`、`PROJECT.md` 和 `TASKS.md`，了解方向、约束、当前任务及交接信息。
- 以用户最新明确指令为准。所有开发在当前文件夹及子目录内进行，文档默认使用中文。
- 用户指定任务时直接推进；未指定时根据上下文选择下一项可执行任务。候选想法不自动视为已确认范围。

## 模型与 Token 规划

- 实质性工作执行前先拆分步骤，并为分析、实现、检查等步骤选择能力合适的模型。
- 文件查找、格式整理、简单修改和摘要等常规工作，优先使用速度快、Token 性价比高的模型。
- 架构设计、复杂代码、疑难问题、关键工程判断和最终复核，使用能力足够的模型；高风险结果可由更强模型复核。
- 通过集中读取、复用已有结论和避免重复输出减少 Token，不通过省略必要分析、测试或验证来节省成本。
- 简单任务可在执行前用一句话说明模型安排；复杂任务在 `TASKS.md` 记录简短的步骤与模型计划。若无法切换模型，应继续使用当前模型，并加强验证。

## 执行中

- 实质性工作在 `TASKS.md` 建立或复用任务编号，开始时标记“进行中”。
- 依据需求及完成标准实施，复杂任务先写简短步骤，小任务直接完成。
- 新需求和关键决定更新 `PROJECT.md`；新增想法放入 `TASKS.md` 收集区，安排后移入任务表。
- 保持小而完整的变更，不覆盖无关用户工作，不无故增加管理文件或流程。
- 不提交真实芯片资料和密钥；保留工程数据来源、单位及条件，不虚构 EDA 工具验证结果。

## 开源参考与开发确认

- 新项目与新增功能先不写代码、不搭脚手架，也不默认从零开发；先通过 GitHub 搜索开源项目、Skill 或类似解决方案，优先近期维护、文档完整、社区使用较多且符合本地内网要求的候选，Star 不作为唯一判断。
- 按 `REFERENCES.md` 的十项标准分析维护日期、Star/Fork/Issue/提交与弃用迹象、普通用户部署难度、环境/依赖/服务、现成可用/可改造/需新开发功能、安全风险、许可证与综合适配；记录日期、具体版本和证据，未知项不得推断为安全或已验证。
- 调研后按 `PROJECT.md` 比较 A 直接使用、B 二次开发、C 从零开发，明确首选及理由，再给出只保留当前必要核心流程的 MVP 和不做项。先提交分析并等待用户确认方案及 MVP 范围；未确认不进入开发或安装运行。未找到合适项目也不自动启动从零开发。
- 在许可证和安全检查允许的范围内参考或复用代码并进行扩展；许可证不兼容时只参考公开的产品思路，不复制相关代码。
- 每个参考项目都更新 `REFERENCES.md`；清单保留名称、网址、调研日期、参考内容、许可证、安全检查和使用版本，重点候选补齐十项分析与比较结论，历史记录不冒充本次实时复核。
- 如需下载参考代码，只放入项目根目录的 `reference-sandbox/` 隔离区。检查来源、文件内容、依赖、脚本和明显恶意行为前，不运行代码或安装依赖。
- 安全初查覆盖 Cookie、账号密码、API Key、SSH Key、环境变量读取，安装/启动/更新脚本、动态下载执行、权限、遥测及陌生服务器访问；说明读取目的、必要性、数据去向与关闭办法，区分正常所需访问和可疑行为。不使用真实秘密进行试验；静态未发现明显问题不等于完整安全审计通过。
- 发现恶意或可疑代码时立即停止执行；先在隔离区移除或修复风险并重新检查，通过后才能开展其他操作。
- 每版开发完成后，只删除 `reference-sandbox/` 内该版本下载的参考副本，不删除用户文件或项目实现；项目名称和网址继续保留在 `REFERENCES.md`。

## 产品与发布边界

- 产品必须按本地客户端架构开发，成品在公司内网运行，不依赖外网，不扩展到具体专业 EDA 软件。
- 架构和数据设计应支持后续升级与迭代。界面保持清楚美观，小功能可合理合并到同一界面。
- Demo 与正式交付分别放在不同目录。Demo 应覆盖全部功能；缺少演示文件时可下载安全、准确且经过验证的文件。
- 开发和迭代过程不得标记为正式版本。只有用户明确确认完成的版本才可标记正式版本。
- 每次经用户明确确认的正式版本，必须按 `PROJECT.md` 的发布流程上传到统一 GitHub 仓库 `Dr-xiaowei/ic-engineer-workbench`：对应源码提交、唯一版本标签、普通版与 Demo、发布说明和 SHA-256 清单；远端核对成功后才标记发布完成。授权或网络阻塞时如实记录待发布，不省略上传步骤。
- 普通开发提交和文档整理不自动新建正式版本；不得为清理过程文件而删除或改写已发布的正式标签、Release 或附件。
- 新迭代使用独立 Git 分支；打包输出按配置版本分目录且拒绝覆盖已有双包。目录迁移或整理完成后检查相对链接、资源路径、正式包校验和及相关回归，不把历史测试记录当作本次验证。
- 正式版本交付前检查功能、安全和可移植性，并清除个人信息、密钥、绝对开发路径、用户名及开发电脑相关痕迹。

## 完成后

- 执行与改动相关的检查；文档检查内容和链接，代码验证相关功能。
- 对关键结论检查证据与准确性；发现模型输出不确定、前后矛盾或依据不足时，重新分析或使用更合适的模型复核。
- 符合完成标准才标记“已完成”，在任务备注留下简短验证结果。未验证或被阻塞时如实记录。
- 更新 `TASKS.md` 的工作交接；使用方式变化更新 `README.md`。
- 已完成任务按有意义的阶段归并，细小过程不再逐项累计；需求编号、验收证据、重要决定与未解决问题必须保留。开发经验集中写入复盘，不另建重复归档或流水文件。
- 清理前检查具体路径及引用，仅删除可重建缓存、临时导出/渲染、参考副本和系统元数据；保留源码、锁文件、测试、许可证、必要 Demo 输入、最终交付物及 Git 历史，不删除用户原件、运行数据或备份。
- 只有影响功能、使用方式、关键技术、数据兼容性、重要问题修复或项目管理规则的变化才写入 `CHANGELOG.md`。错别字、格式调整、微小重构和无行为变化的简单修改不记录。
- 每次更新 `CHANGELOG.md` 前先通读现有记录，合并重复、连续或同主题内容。凝练时保留重要结果、影响及必要的日期或版本信息，不丢失仍有价值的更新内容。
- 向用户简要说明完成内容、验证结果和未解决事项。
