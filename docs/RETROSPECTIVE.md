# v1.0.0 开发复盘与迭代交接

日期：2026-09-06。用户确认首版，源码采用 MIT。版本号表示交付基线，不表示公司服务或所有平台均已验收。

## 本次成果

Tauri 2、React/TypeScript、Rust 和 SQLite 本地架构，11 个主导航入口，两套独立应用。前端 36 项、Rust 30 项、Demo 协议 4 项测试组成当前回归基线；具体证据和限制见 COMPLETION_REPORT.md。

## 开发过程（按关键阶段）

| 阶段 | 主要工作与决定 | 留下的成果 |
| --- | --- | --- |
| 方向与范围确认 | 明确面向 IC 研发、本地内网、不接入 EDA；调研成熟项目并由用户确认功能 | 需求编号、开源参考、权限及许可证边界 |
| 架构与核心实现 | 采用 Tauri/React/Rust/SQLite 分层；先打通客户端、模型、项目、文档与邮件 | 本地数据与受控服务基础，主工作流 |
| 需求扩展 | 按用户要求增加项目进度、日历、计算器、软件管理，再完善历史、PDF、笔记和体验 | R-014 至 R-026 对应功能，独立合成 Demo |
| 对照验收与补缺 | 逐项复核需求，结合自动测试与真实桌面/导出检查修复使用问题 | 完成度报告及下表可复用经验；明确公司验收未完成项 |
| 净化与正式交付 | 用户确认 v1.0.0 和 MIT；清除开发信息与缓存，打包并上传 GitHub | 唯一正式标签、普通版/Demo、校验清单、README、案例和复盘 |

这些阶段说明“为何这样做、最终留下什么”，不保留无价值的逐次修改、测试计数增长或登录重试流水。发布后管理文件整理不计为新的产品版本。

## 问题与经验

| 发现的问题 | 本次处理 | 后续保留的规则 |
| --- | --- | --- |
| PDF 能解析但桌面阅读空白 | 本地 PDF.js、worker、字体/CMap；静态绘制不依赖后台 RAF | 必须检查目标 WebView 中实际可见内容 |
| DOCX 只是装入 Markdown 字符串 | 改为标题、强调、列表、表格、图片的结构化导出，实际导出后渲染 | 检查内容与版式，不能只看扩展名和 ZIP 结构 |
| PDF 图片变成 base64 源码 | 增加内嵌图片和可搜索正文测试、视觉检查 | 为导出保留代表性图文样例 |
| 清理后编译引用失效 | Demo 改为引用版本控制中的集中合成资料 | 清理后重建，禁止依赖临时输出路径 |
| 切换笔记丢失编辑，映射只覆盖后续保存 | 笔记切换保护和既有笔记初始化映射 | 保存、切换、删除、目录不可用分别测试 |
| 源码无隐私但二进制有路径 | 编译路径重映射与最终包扫描 | 源码检查不能替代交付物检查 |
| 模拟测试被误当作工程验收 | 明确合成数据、协议测试和公司服务联调边界 | 不虚构 EDA 验证、生产联调或效率收益 |
| 浏览器登录不等于发布通道已就绪 | 官方 CLI 设备授权并核对系统网络通道 | 提前检查身份、仓库归属和上传能力 |

## 下一版建议

1. 优先完成公司模型与 IMAP/SMTP 的证书、认证、流式协议、文件夹和已发送行为验收，使用专用测试账户。
2. 确定目标系统/架构和组织签名、公证、升级回退策略；Linux GTK3 维护与健全性提示需独立复核。
3. 扩展复杂图文、合并表格、长表分页、损坏文档和大尺寸图片回归；不要把简单排版宣传为完整 Office 保真。
4. 增加跨模块草稿、异常退出恢复、映射目录离线、并发写入及磁盘满测试；统一事务和错误处理基础设施。
5. 通过相同脱敏任务测量查找耗时、摘要修正次数和失败率，具备可比较样本后才报告业务收益。

以上是后续建议，不自动视为已确认的新开发范围。多人协同、云同步、自动 EDA 等扩展必须重新确认需求。

## 下一次版本更新流程

读取 README、PROJECT、TASKS → 调研并确认需求 → 实现与相关回归 → 桌面/导出与安全复核 → 用户明确确认正式版本 → 对应源码及双包发布 → 远端提交、标签与附件校验 → 更新交接。

每次经用户确认的正式版本均上传同一 GitHub 仓库；具体要求统一以 [PROJECT 发布流程](../PROJECT.md) 为准。具备条件时补做重新下载校验，受阻时明确已完成和未完成的检查，不把服务端摘要核对写成重新下载通过。

每版只保留最终交付与必要源码资料。缓存、临时截图、测试导出和参考下载副本可重建；用户工程原件、运行数据和备份不能按开发垃圾删除。

## 目录隔离补充（2026-09-19）

公司内网平台整体迁入 `company-intranet-workbench/`，内部相对路径保持稳定，外层管理共用开发规范和各项目入口；具体产品需求及技术、发布约束留在项目内。提炼共用方法时不携带旧项目功能或验收状态，各项目仍保留独立克隆所需的完整执行基线。迁移前保存文档快照，迁移后核对文件哈希、Git 历史与正式包，再执行测试和真实窗口检查；避免把“能编译”当成全部功能验收。

双包脚本原来固定写入 v1.0.0，后续版本有覆盖旧应用的风险，现按配置版本选择目录并拒绝覆盖。外置盘的 AppleDouble 元数据曾干扰 Git 完整性检查，核实附属文件头后仅清理系统元数据，保留真实 Git 对象。公司服务和跨平台验收继续独立记录。
